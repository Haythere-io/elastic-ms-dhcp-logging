  "aliases": {
    "microsoft.dhcp":{
      "is_write_index": true
    }
  },
  "settings": {
    "index": {
      "number_of_shards": 1,
      "number_of_replicas": 1
    },
    "lifecycle" : {
      "name" : "microsoft.dhcp",
      "rollover_alias" : "microsoft.dhcp"
    },
    "mapping" : {
        "total_fields" : {
          "limit" : "10000"
    }
  }
}
